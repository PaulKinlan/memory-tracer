# memory-audit

Audit a URL for memory leaks (test plan → heap snapshots → memlab → report).

Read the file `.claude/skills/memory-audit/SKILL.md` and follow its
instructions exactly. Treat the text the user typed after the command as the
arguments (the URL, plus optional flags like `--plan-only`, `--scenario <id>`,
`--out <dir>`).
